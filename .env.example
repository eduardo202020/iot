# MuseRAG runs locally from the curated MVP cards by default. Set "remote" only
# when the phone can reach an active MuseRAG server on the LAN.
EXPO_PUBLIC_MUSERAG_MODE=local

# MuseRAG API URL, used when EXPO_PUBLIC_MUSERAG_MODE=remote
EXPO_PUBLIC_MUSERAG_URL=http://localhost:8000
